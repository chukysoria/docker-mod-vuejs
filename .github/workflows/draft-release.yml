name: 'Release Drafter'

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  update_release_draft:
    name: Release
    uses: chukysoria/workflows/.github/workflows/new-release.yml@7ff21d108b6b8eefbae4b571409b7905dc08206f # v1.1.25
    with:
      simple-build: true
